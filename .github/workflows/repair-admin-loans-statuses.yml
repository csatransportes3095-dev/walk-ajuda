name: Repair Admin Loans Statuses

on:
  push:
    paths:
      - '.github/workflows/repair-admin-loans-statuses.yml'
      - 'scripts/repair-admin-loans-statuses.cjs'

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply status fixes
        run: node scripts/repair-admin-loans-statuses.cjs
      - name: Commit fixes and cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/repair-admin-loans-statuses.cjs .github/workflows/repair-admin-loans-statuses.yml
          git add server/routers/loans.ts client/src/pages/AdminLoans.tsx
          git commit -m "fix: alinha status e atualização do ADM empréstimos"
          git push origin HEAD:main
