name: Product Manifest Acceptance Legacy Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legado desativado. A implementacao valida esta no codigo da branch."
